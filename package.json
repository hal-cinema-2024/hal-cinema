{
  "name": "hal-cinema",
  "version": "1.0.0",
  "description": "## How to development?",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "mock": "concurrently \"npm run mock:movie\" \"npm run mock:order\" \"npm run mock:schedule\" \"npm run mock:user\"",
    "mock:movie": "json-server --watch mock/movie.json -p 3055",
    "mock:order": "json-server --watch mock/order.json -p 3056",
    "mock:schedule": "json-server --watch mock/schedule.json -p 3057",
    "mock:user": "json-server --watch mock/user.json -p 3058"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aspida/axios": "^1.14.0",
    "aspida": "^1.14.0",
    "axios": "^1.7.2",
    "json-server": "^1.0.0-beta.3",
    "qs": "^6.12.1",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/qs": "^6.9.15"
  }
}
